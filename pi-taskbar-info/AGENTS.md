# pi-taskbar-info guidance

- Keep this extension self-contained and installable with `pi install /path/to/pi-taskbar-info`.
- Preserve the custom footer behavior unless the task explicitly changes it.
- Update `README.md` when footer fields or install steps change.
- Add or update tests for formatting and footer behavior when practical.
